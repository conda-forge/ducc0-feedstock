microarch_level:
  - 1
  - 3  # [unix and x86_64]
  # linux-64 only: conda-forge's CI provides AVX-512 (emulation + __archspec
  # override) for level 4 on linux-64, but not on the osx-64 runners.
  - 4  # [linux and x86_64]
